[project]
name = "production-rag-platform"
version = "0.1.0"
description = "Production-grade RAG platform: Milvus, FastAPI, RAGAS evaluation, Kubernetes, CI/CD and observability."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pymilvus>=2.4.0",
    "chromadb>=0.5.0",
    "openai>=1.40.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.3.0",
    "numpy>=1.26.0",
    "tqdm>=4.66.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2.0",
    "ruff>=0.5.0",
    "pre-commit>=3.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["api"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
markers = [
    "integration: requires a running Milvus instance (docker compose -f deploy/compose/milvus.yml up -d)",
]
